import classNames from 'classnames/bind';
import styles from './Explore.module.scss';

const cx = classNames.bind(styles);

const ExplorePage = () => {
    return <div className={cx('wrapper')}>ExplorePage</div>;
};

export default ExplorePage;
